package Week01F;

public class silsub {
    public static void main(String[] args) {
        // 섞기(Shuffle)
        // 선생님이 주신 예시다. 분석을 진행해봐야 한다.

        int[] numArr = new int[10];
        for (int i = 0; i < numArr.length; i++) {
            numArr[i] = i;
            System.out.println(numArr[i]);
        }
        System.out.println();

        for (int i = 0; i < 100; i++) {
            int n = (int)(Math.random()*10);
            
        }


    }
}
